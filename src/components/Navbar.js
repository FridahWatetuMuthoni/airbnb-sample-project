import logo from '../images/air-logo.png';

const Navbar = () => {
    return (
        <nav className="nav">
            <section className='navbar'>
                <div className="logo">
                    <img src={ logo} alt="" />
                </div>
        </section>
        </nav>
        )
}

export default Navbar;